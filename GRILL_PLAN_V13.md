# GRILL PLAN V13 — ครอปแนวตั้งอย่างเดียว + ล็อกรูปไม่ให้ขยับตอนเลื่อนหน้า

วันที่: 2026-07-28
ต่อจาก [GRILL_PLAN_V12.md](GRILL_PLAN_V12.md) — รอบนี้มีรูปหน้าจอจริงจาก iPhone ประกอบ

## โจทย์ (จากรูปจริง)
1. ภาพที่ครอบ**ขาด** — ตารางถูกตัดคอลัมน์ขวาหายไป → **ไม่ต้องครอบซ้ายขวา ครอปแต่บนล่าง**
2. ด้านบนยังไม่ครอป — พื้นไม้กับข้อความแดงยังอยู่เต็ม → ต้องครอปลงมาถึงตารางชั่ง
3. ตอนเลื่อนหน้าจอหลังกดดูใบชั่ง รูปขยับตาม → ต้องล็อกอยู่กับที่

---

# ข้อ 1+2 — เปลี่ยนเป็นครอปแนวตั้งอย่างเดียว

## ทำไม V12 ถึงพัง
V12 หากรอบตาราง**ในกรอบกระดาษ**ที่หามาได้ แต่ในรูปจริงพื้นไม้สว่างพอที่จะกลายเป็น
"ก้อนสว่างที่ใหญ่ที่สุด" เสียเอง การหาเส้นจึงไปทำงานผิดที่ ได้กรอบแคบ ๆ ที่กินทั้งไม้และใบชั่ง

## สิ่งที่แก้ — `findTableBox()` → `findTableRows()`
1. **คืนแค่ช่วงแถวบน-ล่าง** ส่วนแนวนอนใช้เต็มความกว้างรูปเสมอ (`minX = 0, maxX = w-1`)
   ตัดปัญหาตารางขาดคอลัมน์ทิ้งทั้งหมด
2. **หาเส้นจากทั้งรูป** ไม่ผูกกับกรอบกระดาษอีกต่อไป
3. เกณฑ์แยกเส้นตารางออกจากพื้นหลัง ใช้ 2 อย่างคู่กัน:
   - **ยาว** (ช่วงเข้มติดกัน ≥ 35% ของความกว้าง) → ตัดตัวหนังสือ/ข้อความแดงบนรูปที่เป็นช่วงสั้น ๆ ออก
   - **บาง** (หนาไม่เกิน 3% ของความสูงรูป) → ตัดพื้นไม้/เงา/แถบมืดทึบ ที่เข้มยาวเต็มแถวเหมือนกันแต่หนาเป็นร้อยแถว
4. **ขยายกลับไปหาแถบทึบที่อยู่ชิด ๆ**: เส้นแรก/สุดท้ายของตารางมักติดขอบกระดาษจนถูกกลืนรวมเป็นแถบทึบ
   ทำให้แถวหัวตารางหายไป ถ้ามีแถบทึบอยู่ห่างไม่เกิน 1.5 เท่าของระยะห่างเส้นปกติ ให้ขยายไปจนถึงขอบแถบนั้น
   (ก่อนใส่ข้อนี้ เคสรูปจริงได้สูง 401 แทนที่จะเป็น 450 — หัวตารางขาด)
5. หาเส้นไม่เจอ → fallback ใช้ช่วงแนวตั้งของกรอบกระดาษ แต่ยังเต็มความกว้างเสมอ

## ผลทดสอบ (ทุกเคส 900×1600)
| เคส | ควรได้ | ได้จริง | เต็มความกว้าง |
|---|---|---|---|
| พื้นไม้ + ข้อความแดง + ใบชั่งอยู่ล่าง (จำลองรูปจริง) | สูง ~450 | **900×451** ✅ | ✅ |
| ใบชั่งอยู่กลาง มีไม้ทั้งบนและล่าง | สูง ~600 | **900×601** ✅ | ✅ |
| ใบชั่งเอียง ~1.7 องศา | สูง ~500 | **900×493** ✅ | ✅ |
| กระดาษเต็มเฟรม มีหัวกระดาษ + ลายเซ็น | สูง ~600 (ตัดหัวกระดาษ) | **900×630** ✅ | ✅ |
| ไม่มีเส้นตารางเลย → fallback | กว้างเต็มเสมอ | **900×523** ✅ | ✅ |
เวลา 33–89 ms, ไม่มี console error
ยืนยันด้วย screenshot: ครอปเริ่มที่แถวแรกของตาราง ("28 ก.ค. 2569 | บังดิเรก") พอดี ไม้กับข้อความแดงหายหมด

---

# ข้อ 3 — ล็อกรูปไม่ให้ขยับตอนเลื่อนหน้า

## ต้นเหตุ
V11 ผูก `positionSlipModal()` ไว้กับ event `resize`
บน iOS แถบ URL ยุบ/ขยายตอนเลื่อนหน้าจะยิง `resize` ตลอด → ฟังก์ชันนี้ทำงานซ้ำ
ซึ่งข้างในมีทั้ง `window.scrollTo()` (ดึงหน้ากลับ) และคำนวณความสูง overlay ใหม่จากตำแหน่งการ์ดที่ขยับไปแล้ว
รูปเลยขยับ/เปลี่ยนขนาดตามการเลื่อน

## สิ่งที่แก้
เปลี่ยนจาก `resize` เป็น `orientationchange` เท่านั้น (หมุนจอจริงถึงจะจัดตำแหน่งใหม่)
เปิดแล้วเรขาคณิตล็อกค้างไว้ ไม่แตะอีก

## ผลทดสอบ (เลื่อนหน้า 2 ครั้ง ยิง resize คั่นทุกครั้ง)
| | ก่อน | หลัง |
|---|---|---|
| ตำแหน่งรูป (top/bottom) | 59 / 243 | **59 / 243** ล็อกอยู่กับที่ ✅ |
| ความสูง overlay | 301.656px | **301.656px** คงเดิม ✅ |
| scrollY | 73 | 373 (เลื่อนได้ครบ 300 ไม่ถูกดึงกลับ) ✅ |
