# GRILL PLAN V11 — popup ดูใบชั่ง: ครอปให้ตรงใบ + เอา chrome ออก + วางเหนือตาราง

วันที่: 2026-07-28
ต่อจาก [GRILL_PLAN_V10.md](GRILL_PLAN_V10.md) (แก้บั๊กจอค้าง)

## โจทย์
1. ครอปยังไม่ตรง — ไม่ได้ครอบเฉพาะใบชั่งสีขาว
2. popup ไม่ต้องมีคำ ไม่ต้องมีปุ่ม ให้เหลือแค่รูป
3. เปิด popup แล้วให้ **ขอบล่างของรูปอยู่เหนือตาราง "น้ำหนักรถและสินค้า"** เพื่อเทียบตารางในรูปกับในรายงานได้
4. (เพิ่มระหว่างทาง) ขอความชัดของรูปเพิ่ม

---

# Q1 — วิธีหาใบชั่งในรูป

## ปัญหาที่พิสูจน์ได้
`autoCropWhiteDocument()` เดิมเก็บ min/max ของพิกเซลที่ `lum > 140` **ทุกจุดในรูป**
→ ของสว่างคนละมุม (มือ, กระดาษอีกใบ, แสงสะท้อน) ทำให้กรอบพองจนเกือบเท่ารูปเดิม

ทดสอบ: รูป 1200×1600, ใบชั่งขาว 600×700 อยู่กลาง + กระดาษมุมบนซ้าย + แสงสะท้อนมุมล่างขวา
→ **ครอปได้ 1142×1510** (แทบเท่ารูปเดิม)

## การตัดสินใจ
เลือก **ตัวเลือก (ก)**: หาก้อนสีขาวที่ใหญ่ที่สุดด้วย connected-component
เหตุผล: แก้ตรงต้นเหตุ, เป็น JS ล้วน ไม่พึ่ง network ไม่กินโควต้า Gemini
(สำคัญเพราะปุ่มนี้มีไว้ครอสเช็คตอนโควต้าหมดพอดี)

## สิ่งที่แก้ (`autoCropWhiteDocument`)
1. **Otsu threshold** แทนค่าคงที่ `140` — ปรับตามแสงของรูปเอง
2. **Dilate mask รัศมี 2** ก่อนหาก้อน — ใบชั่งมีตารางที่เส้นขอบพาดเต็มความกว้าง
   ถ้าไม่ dilate เนื้อขาวจะถูกหั่นเป็นแถบ แล้วก้อนที่ใหญ่สุดจะเหลือแค่แถบเดียว → ครอปกินใบไม่หมด
3. **Flood fill (4-connectivity) หาก้อนที่ใหญ่ที่สุด** แล้วใช้กรอบของก้อนนั้น
   ของสว่างมุมอื่นถูกทิ้งเพราะเป็นคนละก้อนและเล็กกว่า
4. เกณฑ์ผ่าน: ก้อนต้องใหญ่ > 2% ของรูป และกรอบ > 15% ของทั้งกว้างและสูง (ไม่ผ่าน → คืนรูปเต็ม)

## ผลทดสอบ (รูป 1200×1600 ทั้งหมด)
| เคส | ใบชั่งจริง | ครอปได้ | เวลา |
|---|---|---|---|
| ใบชั่งเปล่า + ของสว่าง 2 มุม | 600×700 | **621×722** ✅ (เดิม 1142×1510) | 49 ms |
| ใบชั่งมีตารางเส้นเข้มพาดเต็มความกว้าง | 700×850 | **720×872** ✅ | 28 ms |
| ใบชั่งเอียง + แถบสีมือสว่างด้านล่าง | 700×850 (เอียง) | **770×911** ✅ (กรอบของสี่เหลี่ยมเอียงย่อมใหญ่กว่าตัวมันเอง) | 77 ms |
ส่วนเกิน ~20px คือ padding 15px + dilate 2px ตามที่ตั้งใจ

---

# Q2 — เพิ่มความชัดของรูป

## การตัดสินใจ
เลือก **ตัวเลือก (ค)**: เพิ่มขนาด + ให้ซูมนิ้วในรูปได้ (ไม่เพิ่มคอนทราสต์)
ซูมนิ้วเป็นท่าสัมผัส ไม่ต้องเพิ่ม UI สักชิ้น จึงไม่ขัดกับข้อ 2 ที่ขอว่าไม่เอาปุ่ม

## สิ่งที่แก้
1. `OUT_MAX` 1600 → **2000**, JPEG 0.92 → **0.95**
   พอครอปตรงใบชั่งแล้ว ความละเอียดนี้ถูกใช้กับเนื้อใบชั่งล้วน ๆ (เดิมถูกใช้กับทั้งรูปถ่าย)
2. เพิ่มระบบ **ซูมนิ้ว** ในรูป (`initSlipZoom`) — ไม่มีปุ่มใด ๆ
   - pinch 2 นิ้ว = ซูม 1x–5x
   - ลากนิ้วเดียวตอนซูมอยู่ = เลื่อนดู
   - แตะสองครั้ง = สลับ 1x ↔ 2.5x
   - ล้อเมาส์ = ซูม (desktop)
   - `touch-action:none` บนรูป กันเบราว์เซอร์แย่งท่าสัมผัสไป
3. ธง `slipGesture` กันไม่ให้ท่าซูม/ลากที่จบแล้วนิ้วหลุดไปโดนพื้นหลัง แล้ว popup ปิดเอง (ปล่อยธงหลัง 350ms)
4. `resetSlipZoom()` ทุกครั้งที่เปิดและปิด popup

## ผลทดสอบ (รูป 2400×3200, ใบชั่ง 1400×1700 + กระดาษอื่นมุมบนซ้าย)
| | ผล |
|---|---|
| ครอปได้ | **1416×1719** ✅ ตรงใบชั่ง |
| ขนาด data URL | 66 KB |
| pinch กาง 2 เท่า | `scale(2)` ✅ |
| ลากตอนซูม | `translate(40px, 60px)` ✅ |
| ปิด popup | ซูมกลับ 1x, transform reset ✅ |
| ซูมจบแล้วนิ้วโดนขอบดำทันที | ไม่ปิด ✅ |
| ผ่านไป 400ms แล้วแตะขอบดำ | ปิด ✅ |
| แตะรูปเฉย ๆ (ไม่ซูม) | ไม่ปิด, ไม่ตั้งธง ✅ |
| console errors | ไม่มี |

หมายเหตุ: ใบชั่ง 1416px < 2000px จึงไม่ถูกย่อเลย — เก็บรายละเอียดเต็ม (ของเดิมจะถูกบีบเหลือ 1600px)

---

# Q3 — เอาคำและปุ่มออกจาก popup

## การตัดสินใจ
เลือก **ตัวเลือก (ค)**: เอา chrome ออกทั้งหมด + พึ่ง fallback อัตโนมัติที่มีอยู่แล้ว
(ครอปไม่ผ่านเกณฑ์ → `currentSlipCroppedUrl` ถูกตั้งเป็นรูปเต็มตั้งแต่ตอนเตรียมรูป)

## สิ่งที่แก้
1. HTML: ลบการ์ดขาว, แถบหัว ("📄 ภาพใบชั่งน้ำหนัก"), ปุ่มครอบ/เต็ม, ปุ่ม ✕, แถบท้าย
   เหลือ `#slipViewModal > img#slipModalImg` เท่านั้น
2. JS ที่ตายไปพร้อมกัน — ลบทิ้ง: `toggleSlipCropMode()`, ตัวแปร `isCroppedMode`,
   `safeTap('btnToggleCrop')`, `safeTap('btnCloseSlipModal')`
3. `updateSlipModalImageSrc()` เหลือบรรทัดเดียว: `img.src = currentSlipCroppedUrl || currentSlipOriginalUrl`

## ผลทดสอบ
| | ผล |
|---|---|
| จำนวน `<button>` ใน popup | **0** ✅ |
| ข้อความใน popup | **ไม่มี** ✅ |
| ลูกของ overlay | `IMG#slipModalImg` ตัวเดียว ✅ |
| แสดงรูปครอป | ✅ |
| console errors | ไม่มี |
ยืนยันด้วย screenshot: เหลือแค่ใบชั่งลอยบนพื้นมัว

---

# Q4 — ตำแหน่งรูป + พื้นหลังไม่ทึบดำ

## ปัญหา
ปุ่ม ดูใบชั่ง อยู่ใน**หัวการ์ด STEP 3 เอง** ตอนกด หัวการ์ดจะอยู่ตรงไหนของจอก็ได้
ถ้าไม่เลื่อนหน้าก่อน แล้วบังเอิญหัวการ์ดอยู่บนสุด รูปจะเหลือที่แสดงแทบไม่มีเลย

## การตัดสินใจ
เลือก **ตัวเลือก (ก)** + **พื้นหลังโปร่งใส ไม่ทึบดำ**

## สิ่งที่แก้
1. `positionSlipModal()` — เลื่อนหน้าให้การ์ด "น้ำหนักรถและสินค้า" มาอยู่ที่ ~50% ของจอ
   แล้วอ่านตำแหน่งจริง**อีกรอบหลังเลื่อน** (หน้าอาจเลื่อนได้ไม่สุดตามที่ขอ)
   ตั้งความสูง overlay = `max(30% ของจอ, ระยะถึงหัวการ์ด − 8px)`
2. overlay: `background:transparent`, ลบ `backdrop-filter:blur(6px)` ทิ้ง
   — ต้องอ่านข้อมูลในรายงานได้ขณะเทียบ
3. รูป: ขอบขาว 3px + เงา เพื่อให้แยกตัวออกจากเนื้อหาหน้าเว็บได้โดยไม่ต้องพึ่งพื้นมืด
4. **เลิกล็อก scroll** (`body.overflow='hidden'`) — ตารางต้องเลื่อนดูได้ขณะเทียบ
   ผลพลอยได้: ตัดความเสี่ยงจอค้างแบบ V10 ออกจากสมการทั้งหมด
5. `resize` listener จัดตำแหน่งใหม่เมื่อหมุนจอ
6. `closeSlipModal()` ล้าง `height`/`bottom` เพื่อให้เปิดครั้งถัดไปคำนวณใหม่

## ผลทดสอบ (จอสูง 619px, เคสร้ายสุด: เลื่อนจนหัวการ์ดอยู่ y=4 แล้วกดปุ่ม)
| | ผล |
|---|---|
| หัวการ์ดก่อนกด | y = 4 (ชิดบนสุด) |
| หลังเลื่อนอัตโนมัติ | y = **310** (≈50% ของจอ) ✅ |
| overlay | 0 → 302 px |
| ขอบล่างรูป | y = 292 → **อยู่เหนือหัวการ์ด** ✅ |
| พื้นหลัง overlay | `rgba(0,0,0,0)` โปร่งใส ✅ |
| backdrop blur | `none` ✅ |
| scroll ล็อกไหม | ไม่ล็อก ✅ |
| แตะช่องในตาราง | โดน `TD` จริง overlay ไม่บัง ✅ |
| เลื่อนหน้าขณะเปิด | เลื่อนได้ รูปยังปักอยู่ที่เดิม ✅ |
| แตะรูป / แตะพื้นข้างรูป | ไม่ปิด / ปิด ✅ |
| console errors | ไม่มี |

ยืนยันด้วย screenshot: ใบชั่งลอยครึ่งบน ตารางน้ำหนักครึ่งล่างอ่านได้ชัดเต็มที่ ไม่มีพื้นมืดทับ
