# GRILL PLAN V12 — ครอปเฉพาะตารางชั่ง + toggle ปุ่ม + แตะช่องว่างให้ปิด

วันที่: 2026-07-28
ต่อจาก [GRILL_PLAN_V11.md](GRILL_PLAN_V11.md)

## โจทย์
1. ครอปด้านล่างตรง แต่ด้านบนไม่ครอปเลย — ต้องครอบเฉพาะ**ตารางชั่งน้ำหนัก**
2. ปรับให้ภาพที่ครอบกว้างขึ้น
3. กดปุ่ม ดูใบชั่ง ซ้ำขณะรูปโชว์อยู่ → รูปต้องหาย
4. แตะบางจุดที่เป็นช่องว่างแล้วรูปยังไม่หาย

---

# ข้อ 3 — กดปุ่มซ้ำแล้วรูปหาย (ทำเลย ไม่ต้องถาม)
`btnViewSlip` เปลี่ยนจากเรียก `openSlipModal` เป็น `toggleSlipModal()` (ทั้ง inline onclick และ safeTap)
ทดสอบกด 4 ครั้งติด: โชว์ → หาย → โชว์ → หาย ✅

---

# Q1 — หา "ตารางชั่งน้ำหนัก" ในรูปด้วยวิธีไหน

## ปัญหาที่พิสูจน์ได้
อัลกอริทึม V11 ครอบ **แผ่นกระดาษ** ไม่ใช่ตาราง
ถ้าถ่ายใกล้จนกระดาษล้นเฟรมด้านบน/ซ้าย/ขวา จะเหลือขอบล่างด้านเดียวที่ตัดได้
ทดสอบ: รูป 1200×1600, ตารางจริงอยู่ที่ 1000×600 → **ครอปได้ 1200×1318** (บนไม่ตัดเลย ล่างตัดตรง)
ตรงกับอาการที่รายงานมาเป๊ะ

## การตัดสินใจ
เลือก **ตัวเลือก (ก)**: หาเส้นขอบตารางที่พาดยาว แล้วใช้กรอบของเส้นเหล่านั้น

## สิ่งที่แก้ — เพิ่ม `findTableBox()` ต่อท้ายการหากรอบกระดาษ
ทำงาน**ภายในกรอบกระดาษ**ที่หาได้แล้วเท่านั้น (กันโต๊ะ/พื้นหลังมารบกวน) แล้ว:
1. **หดกรอบเข้ามา 2%** ก่อนเริ่ม — ขอบกระดาษที่ต่อกับพื้นหลังเบลอเป็นสีกลาง ๆ ที่เข้มกว่า threshold
   ทั้งแถวเลยถูกนับเป็นเส้นตาราง แล้วลากกรอบไปจนสุดขอบกระดาษ (เจอตอนทดสอบรอบแรก)
2. **ขยายมาสก์ตามแนวตั้ง ±3px** ก่อนหาเส้นแนวนอน — เผื่อรูปเอียงเล็กน้อย เส้นเอียงจะยังนับได้เต็มแถว
3. **วัด "ช่วงเข้มที่ยาวติดกันที่สุด" ไม่ใช่จำนวนพิกเซลเข้มรวมทั้งแถว**
   เส้นขอบตารางเป็นเส้นทึบยาวช่วงเดียว ส่วนหัวกระดาษเป็นตัวหนังสือช่วงสั้น ๆ หลายช่วง
   ถ้านับรวม ชื่อบริษัทตัวใหญ่จะถูกนับเป็นเส้นตารางด้วย (เจอตอนทดสอบรอบสอง)
4. แถวที่ช่วงยาวเกิน 35% ของความกว้าง = เส้นขอบตาราง ต้องเจอ **อย่างน้อย 3 เส้น**
   กรอบแนวตั้ง = เส้นแรกถึงเส้นสุดท้าย
5. เส้นแนวตั้งหาแบบเดียวกันในช่วงความสูงของตาราง ถ้าเจอ ≥2 เส้นใช้เป็นขอบซ้าย-ขวา
   ถ้าไม่เจอ (ตารางมีแค่เส้นคั่นแนวนอน) ใช้ขอบซ้าย-ขวาของหมึกในช่วงนั้นแทน
6. หาเส้นไม่เจอ → คืน `null` → ใช้กรอบกระดาษเดิมต่อ (fallback)

## ผลทดสอบ (รูป 1200×1600 ทุกเคส)
| เคส | ควรได้ | ได้จริง | เวลา |
|---|---|---|---|
| กระดาษล้นเฟรม + หัวกระดาษ + ลายเซ็น (เคสที่รายงานมา) | ~1030×630 | **1030×630** ✅ | 68 ms |
| ตารางมีแค่เส้นแนวนอน ไม่มีเส้นตั้ง | ~1030×630 | **1016×630** ✅ | 50 ms |
| กระดาษอยู่ในเฟรมทั้งใบ | ~830×530 | **831×530** ✅ | 92 ms |
| ในเฟรมทั้งใบ + เส้นแนวนอนอย่างเดียว | ~830×530 | **816×530** ✅ | 28 ms |
| ตารางเอียง ~1.4 องศา | ~1030×630 | **1033×635** ✅ | 56 ms |
| ไม่มีเส้นตารางเลย → fallback | 621×722 (กรอบกระดาษ) | **621×722** ✅ | 73 ms |

## ข้อ 2 (ภาพกว้างขึ้น) ได้มาพร้อมกัน
ตารางชั่งเป็นแนวนอน (1030×630) ต่างจากทั้งใบที่เป็นแนวตั้ง
รูปจึงเต็มความกว้างของจอเอง — วัดได้ **93% ของความกว้างจอ** (เดิม ~42%)

---

# Q2 — แตะช่องว่างแล้วรูปไม่หาย

## ปัญหา
V11 ให้ overlay คลุมแค่ครึ่งบน (เหนือตาราง) ส่วนครึ่งล่างเป็นหน้าเว็บปกติที่แตะทะลุได้
ตั้งใจให้แก้ตัวเลขในตารางได้ขณะเทียบ แต่ผลข้างเคียงคือแตะช่องว่างครึ่งล่างแล้วรูปไม่ปิด

## การตัดสินใจ
เลือก **ตัวเลือก (ข)**: แตะช่องว่างที่ไหนก็ปิด แต่แตะช่องกรอก/ปุ่ม ไม่ปิด

## สิ่งที่แก้
1. ย้าย `closeSlipModalOnBackdrop` ไปเป็น listener ระดับ `document` — ครอบคลุมทั้งจอ ไม่ใช่แค่พื้นที่ overlay
2. ข้อยกเว้นที่ **ไม่** ปิด: ตัวรูปเอง, `#btnViewSlip` (มันสลับเปิด/ปิดเอง),
   และ `input, textarea, select, button, a, label, [contenteditable]`
3. `document.body.style.cursor = 'pointer'` ขณะเปิด (คืนค่าเมื่อปิด)
   — `cursor` เป็น inherited property จึงถ่ายทอดลงทุก element
   จำเป็นบน iOS Safari ไม่งั้นแตะช่องว่างที่เป็น `<div>` ธรรมดาจะไม่เกิด click event เลย

## ผลทดสอบ
| แตะที่ | ผล |
|---|---|
| ช่องกรอกทะเบียนในตาราง | ไม่ปิด ✅ |
| ช่องว่าง `td` ในตาราง | ปิด ✅ |
| หัวข้อการ์ด | ปิด ✅ |
| พื้นหน้าเว็บเปล่า | ปิด ✅ |
| ตัวรูป | ไม่ปิด ✅ |
| พื้นโปร่งข้างรูป | ปิด ✅ |
| ปุ่ม ดูใบชั่ง (กด 3 ครั้ง) | โชว์ → หาย → โชว์ ✅ |
| `body.cursor` ขณะเปิด / หลังปิด | `pointer` / ล้างแล้ว ✅ |

## regression ของ V11 ที่เช็คซ้ำ
| | ผล |
|---|---|
| ซูมนิ้ว | `scale(2)` ✅ |
| ซูมจบแล้วนิ้วโดนพื้น | ไม่ปิด ✅ |
| ผ่านไป 400ms แตะพื้น | ปิด ✅ |
| ขอบล่างรูปอยู่เหนือการ์ด | ✅ |
| เลื่อนหน้าขณะเปิด | ✅ |
| console errors | ไม่มี |
